<% flash.keys.each do |type| %>
  
  <div class="alert alert-<%= alert_type(type) %>">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <span class="message"><%= flash[type] %></span>
  </div>
  
<% end %>